/**
 * Test dependencies
 */

var {{project}} = require("{{project}}")
  , expect = require("expect.js");


/**
 * Demonstrate what {{project}} does with tests.
 */

describe("a {{project}}", function(){

  it("should do something useful", function() {
    expect({{project}}()).to.equal("useful");
  });

});
